/**
 * Legacy type file — PocketBase has been replaced by the mock-db layer.
 *
 * All data types now live alongside their hooks in `src/hooks/`.
 * This file is kept empty so existing imports don't break during cleanup.
 */
